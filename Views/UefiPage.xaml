<Page
    x:Class="BooticeWinUI.Views.UefiPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:BooticeWinUI.Models"
    mc:Ignorable="d">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock x:Uid="Uefi_Title" Text="UEFI Boot Manager" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,20" />

        <!-- Warning / Info -->
        <InfoBar x:Uid="Uefi_Warning" Grid.Row="1" Severity="Informational" IsOpen="True" IsClosable="False" Margin="0,0,0,15"
                 Title="UEFI Firmware Settings"
                 Message="These settings modify the NVRAM of your motherboard. Incorrect changes may prevent your system from booting." />

        <!-- Main Content -->
        <Grid Grid.Row="2" ColumnSpacing="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Boot Order List -->
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                
                <TextBlock x:Uid="Uefi_Order" Text="Boot Order" FontWeight="SemiBold" Margin="0,0,0,10" />
                
                <ListView Grid.Row="1" x:Name="UefiEntriesList" SelectionChanged="UefiEntriesList_SelectionChanged" BorderThickness="1" BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}" CornerRadius="4">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:UefiEntry">
                            <StackPanel Padding="0,5">
                                <TextBlock Text="{x:Bind Description}" FontWeight="SemiBold" />
                                <TextBlock Text="{x:Bind Identifier}" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                
                <CommandBar Grid.Row="2" DefaultLabelPosition="Right" IsOpen="False">
                    <AppBarButton x:Uid="Bcd_Up" Label="Up" Click="MoveUp_Click">
                        <AppBarButton.Icon>
                            <FontIcon Glyph="&#xE74A;" />
                        </AppBarButton.Icon>
                    </AppBarButton>
                    <AppBarButton x:Uid="Bcd_Down" Label="Down" Click="MoveDown_Click">
                        <AppBarButton.Icon>
                            <FontIcon Glyph="&#xE74B;" />
                        </AppBarButton.Icon>
                    </AppBarButton>
                    <!-- <AppBarButton Icon="Delete" Label="Delete" Click="DeleteEntry_Click" /> -->
                </CommandBar>
            </Grid>

            <!-- Entry Details -->
            <ScrollViewer Grid.Column="1">
                <StackPanel Spacing="15" x:Name="EntryDetailsPanel" Visibility="Collapsed">
                    <TextBlock x:Uid="Bcd_Details" Text="Entry Details" Style="{StaticResource SubtitleTextBlockStyle}" />
                    
                    <TextBox x:Uid="Bcd_Description" Header="Description" Text="{x:Bind SelectedEntry.Description, Mode=OneWay}" IsReadOnly="True" />
                    <TextBox x:Uid="Common_Identifier" Header="Identifier" Text="{x:Bind SelectedEntry.Identifier, Mode=OneWay}" IsReadOnly="True" />
                    <TextBox x:Uid="Bcd_DeviceType" Header="Device" Text="{x:Bind SelectedEntry.Device, Mode=OneWay}" IsReadOnly="True" />
                    <TextBox x:Uid="Bcd_Path" Header="Path" Text="{x:Bind SelectedEntry.Path, Mode=OneWay}" IsReadOnly="True" />
                    
                    <TextBlock x:Uid="Uefi_Note" Text="Note: Full editing of UEFI entries via bcdedit is limited. Use 'Up' and 'Down' to change boot order." 
                               FontStyle="Italic" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" TextWrapping="Wrap" />

                    <StackPanel Orientation="Horizontal" Spacing="10" HorizontalAlignment="Right" Margin="0,20,0,0">
                         <Button x:Uid="Uefi_SetTop" Content="Set as First Boot Option" Click="SetTop_Click" />
                         <!-- <Button Content="Disable Entry" /> -->
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
            
            <TextBlock x:Uid="Common_SelectEntry" x:Name="NoSelectionText" Grid.Column="1" Text="Select an entry to view details" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
        </Grid>

        <!-- Status Bar -->
        <InfoBar x:Name="StatusInfoBar" Grid.Row="3" IsOpen="False" Margin="0,10,0,0" />
    </Grid>
</Page>
